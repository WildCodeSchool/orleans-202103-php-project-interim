<form method="post" action="{{ path('legal_notice_delete', {'id': legal_notice.id}) }}" onsubmit="return confirm('Êtes-vous sûr vouloir supprimer cet élément');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal_notice.id) }}">
    <button class="delete btn bg-primary text-light btn-lg w-50">Supprimer</button>
</form>
