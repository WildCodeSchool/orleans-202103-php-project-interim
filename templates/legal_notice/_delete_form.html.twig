<form method="post" action="{{ path('legal_notice_delete', {'id': legal_notice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal_notice.id) }}">
    <button class="btn">Delete</button>
</form>
