{% extends 'base.html.twig' %}

{% block title %}LegalNotice{% endblock %}

{% block body %}
    <h1>LegalNotice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ legal_notice.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ legal_notice.text }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ legal_notice.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('legal_notice_index') }}">back to list</a>

    <a href="{{ path('legal_notice_edit', {'id': legal_notice.id}) }}">edit</a>

    {{ include('legal_notice/_delete_form.html.twig') }}
{% endblock %}
